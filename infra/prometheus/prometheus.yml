global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: prometheus
    static_configs:
      - targets:
          - localhost:9090

  # FastAPI에 /metrics 엔드포인트를 붙인 뒤 활성화한다.
  # - job_name: fleet-backend
  #   static_configs:
  #     - targets:
  #         - host.docker.internal:8081
